# Minimap Stitcher - 中心点截图模式说明

## 新功能介绍

为了提高人物定位准确性，新增了中心点截图模式。该模式允许用户选择人物位置作为截图中心点，并固定截图窗口为正方形，确保人物始终位于图像中心。

## 功能特点

### 1. 中心点选择
- 使用"🎯 选择人物中心点"按钮
- 在游戏画面中点击人物位置
- 系统将以该点为中心进行正方形截图

### 2. 固定正方形截图
- 截图窗口始终为正方形
- 可通过"截图大小"输入框调节边长（默认200像素）
- 范围：100-1000像素

### 3. 自动人物定位
- 在中心点模式下，人物位置自动设为图像中心
- 提高了人物定位的准确性
- 消除了因截图形状不规则导致的定位偏差

## 使用方法

### 模式切换
系统支持两种模式：
1. **区域选择模式**（原有模式）：使用"🖱️ 画框选择区域"
2. **中心点模式**（新模式）：使用"🎯 选择人物中心点"

### 中心点模式操作步骤
1. 点击"🎯 选择人物中心点"按钮
2. 在游戏画面中点击人物所在位置
3. 按ENTER确认选择
4. 在"截图大小"输入框中设置所需边长
5. 点击"▶️ 开始监控"启动拼接

### 参数保存
- 系统会自动保存中心点坐标和截图大小
- 重启程序后会自动加载上次的设置

## 技术改进

### 1. ScreenCapture 类增强
- 新增 `capture_square(center_x, center_y, size)` 方法
- 保证截图始终为正方形
- 保持向后兼容性

### 2. GUI 界面更新
- 新增 CenterPointSelector 覆盖层
- 添加截图大小调节控件
- 支持两种模式切换

### 3. 拼接逻辑优化
- 在中心点模式下，人物位置固定为图像中心
- 提高了地图拼接的准确性

## 配置文件变更

配置文件 `config.json` 现在包含：
- `监控区域`: 原有区域模式的配置
- `监控中心`: 中心点模式的坐标
- `监控大小`: 截图边长

## 兼容性

- 完全向后兼容原有功能
- 现有配置文件可正常使用
- 不影响原有的区域选择模式

## 性能影响

- 正方形截图提高了处理效率
- 固定尺寸减少了计算复杂度
- 人物定位更准确，减少了拼接误差